namespace TypeBeat.Game.Gameplay.Typing
{
    public static class TypingConstants
    {
        // In beatmaps, '/' represents a space/snare.
        public const char SpaceToken = '/';

        // In previews, show space token as '*'.
        public const char PreviewSpaceGlyph = '*';

        // Maximum letters to display in preview lines (as requested).
        public const int PreviewMaxLetters = 4;
    }
}
